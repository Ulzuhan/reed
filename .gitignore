# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Reed runtime data (uploads, embedded Qdrant, sqlite registry)
data/
.env

# The installed CLI needs the built-in evaluation fixture even though runtime
# directories named data/ stay ignored.
!src/reed/evals/data/
!src/reed/evals/data/**

# Evaluation artifacts. Named snapshots under eval/results/ are committed;
# the report each run overwrites, and the judge cache, are not.
eval/results/report.md
eval/results/report.json
eval/results/cache/

# Local model caches
.cache/

# macOS
.DS_Store
